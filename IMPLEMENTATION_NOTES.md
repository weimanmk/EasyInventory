# 实施说明

## 原则应用

- KISS：后端按数据库、迁移、订单、报表、命令分层；前端用统一 API 封装和页面级状态，避免复杂框架叠加。
- YAGNI：V1.0 只做单机本地，不实现账号、云同步、多端和模板可视化编辑。
- DRY：金额格式、数量格式、Tauri 调用、商品/客户全局缓存统一封装。
- SOLID：核心订单规则集中在 `orders.rs`，数据库初始化集中在 `db.rs`，Excel 迁移集中在 `excel.rs`。

## 数据迁移映射

- `库存!B:I` -> `products` 与 `stock_balances`。
- `客户信息!B:D` -> `customers`。
- `明细!B:I` -> `inventory_movements`，入库行同步写入 `inbound_records`。
- `利润` 表只统计可解析历史行数，V1.0 利润以新订单数据为准。

## 业务规则

- 价格优先级：手动价 > 客户商品固定价 > 商品默认售价 > 0。
- 买赠：`floor(购买数量 / 每满数量) * 赠品数量`，赠品 0 元并扣库存。
- 直接折现：作为负数订单行，减少应付和利润，不影响库存。
- 月费：保存订单时生成下月可用额度，抵扣减少客户应付但不减少利润。
- 入库和出库都写库存流水，并重算库存余额和平均进货价。

## 仍需环境完成的事项

- `cargo test` 已通过。
- `npm run tauri:build` 已生成 Windows release EXE 与 NSIS 安装包；MSI 不作为 V1.0 必交付目标。
- 首次启动应用后在设置页执行 Excel 导入并核对商品 280、客户 997、流水 3。
